# Writing Rationale Matrix

| Row ID | Manuscript Unit | Planned Function | Motivation Link | Reference/SOTA Pattern Learned | Target Scene or Venue Norm | User Evidence or Citation Anchor | Planned Text Move | Final Text Check |
|---|---|---|---|---|---|---|---|---|
| F1 | Whole-work framework | Establish the paper's controlling argument before drafting. The paper should not be a generic head-aware compression story; it should be a boundary-aware memory selection story. | The confirmed motivation says memory attention is not memory retrieval. Every section must distinguish memory-region attention, boundary attention, and reusable internal memory. | Forcing-KV stages observation, head profiling, compression policy, and random/manual ablation. We transfer this evidence order, not its static/dynamic head taxonomy. | A CCF-A style VLM paper needs a crisp gap, measurable profiling signal, method tied to the signal, and controlled ablations. | E1-E7, especially E1 for memory-biased readout and E6/E7 as required missing evidence. | Structure the paper as: bottleneck -> memory-biased readout -> boundary contamination -> internal-memory utility -> fixed-budget eviction -> controlled validation. | Pass if a reader can summarize why boundary-aware utility is necessary before seeing the method. |
| I1 | Introduction opening | Anchor the problem in streaming video inference rather than generic KV compression. | Fixed-budget visual KV eviction is the practical bottleneck that creates the need for memory selection. | Existing streaming cache papers usually motivate with latency and memory growth before method details. | Conference introduction should start from task pressure and deployment constraint. | Baseline result files and StreamingBench setting. | Open with chunk-wise streaming video input, repeated KV compression, and the cost of keeping dense visual cache. | Must not begin with head taxonomy before explaining the inference bottleneck. |
| I2 | Limitation of existing methods | Explain why recent-window, uniform attention, and visual-head budget are insufficient. | If memory attention can be boundary-dominated, average attention and visual-head score are incomplete signals. | Strong papers position baselines by failure mode, not by name-dropping. | Need a clear gap that makes MemoSelect inevitable. | E3 baseline table: StreamingVLM Counting drop; visual-head budget weak gain and Causal/Counting drop. | Present the baseline table as evidence of complementary failure modes. | Pass if the paragraph does not overclaim that baselines are poor overall. |
| O1 | Memory-biased readout observation | Establish that retained memory is important enough to study. | The method should not exist unless memory is a dominant readout source. | Forcing-KV first visualizes/quantifies attention patterns before profiling. | Observation section should have direct statistics and figure references. | E1: current token fraction 0.741 vs current attention share 0.242. | State that memory receives disproportionate pseudo-query readout relative to token fraction. | Must avoid saying all heads deeply retrieve memory. |
| O2 | Boundary contamination observation | Introduce the key novelty gap: memory attention can be concentrated around boundary/splice points. | This is the reason utility must be internal-memory and boundary-aware. | Good observation sections turn a simple statistic into a sharper unresolved question. | Needs a diagnostic figure or, until then, careful preliminary wording. | E4 currently diagnostic; F3/F5 needed. | Write as motivation until quantified: whole-cache curves suggest boundary and periodic peaks. | Pass only if wording distinguishes diagnosed phenomenon from completed quantified proof. |
| O3 | Selector utility definition | Define `u_h^int` as the main profiling score. | The score asks whether selected internal-memory tokens are reused by future queries. | Unlike region-mass profiling, this profiles selector reliability rather than attention location. | Method definitions should align exactly with later ablations. | E6 pending. | Define boundary window, internal memory, top-K selection, future readout coverage, random/oracle normalization. | Must explicitly state future readout is offline only. |
| M1 | Online MemoSelect policy | Connect offline utility to online eviction without future leakage. | The offline score becomes a fixed head prior; online inference uses only current pseudo-query attention. | Compression papers need clear deployment-time assumptions. | Method section must separate calibration from inference. | Confirmed motivation and planned experiment design. | Present weighted selector voting, dense fixed budget, current floor, and optional temporal coverage. | Pass if total budget is unchanged and no ragged kernel change is implied. |
| E1 | Baseline comparison table | Show why current baselines motivate the new method. | The table supports the need for reusable memory selection, not final superiority. | Strong papers use baseline failure modes to justify method components. | Completed results can be reported; pending results cannot. | E3. | Include StreamingVLM, HERMES, and visual-head budget prototype. | Must label visual-head budget as prototype/baseline unless user decides it is part of Ours. |
| E2 | Profiling validity experiment | Establish that `u_h^int` is meaningful. | This is the decisive bridge from observation to method. | Forcing-KV uses random/manual profiling ablations; we need top/random/bottom utility validation. | Main paper needs controlled validation of the profiling signal. | E6 pending. | Plan top/random/bottom future coverage and boundary-only control. | Cannot be written as completed until run. |
| E3 | Method validation experiment | Test whether selector-guided eviction improves QA under same budget. | This validates that the profiling signal helps cache management. | Compression papers require same-budget comparisons and random controls. | Must include HERMES, random, bottom, and component ablations. | E7 pending. | Plan MemoSelect-top vs random/bottom/boundary-only/current-floor/HERMES. | Must report exact logs and avoid cherry-picked numbers. |
| D1 | Discussion and limitations | Bound the claims and future work. | Avoid semantic head-role overclaiming. | Mature papers separate behavior-level evidence from mechanistic proof. | Reviewers expect limitations for head profiling and future-query calibration. | Claim register. | State that MemoSelect profiles cache-management behavior, not semantic functions; discuss calibration transfer and pending robustness. | Pass if no unsupported semantic role claim remains. |
